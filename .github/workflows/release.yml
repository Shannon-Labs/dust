name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            platform: linux
            arch: x86_64
            native: true
          - os: ubuntu-latest
            target: aarch64-unknown-linux-gnu
            platform: linux
            arch: aarch64
            native: false
          - os: macos-14
            target: x86_64-apple-darwin
            platform: darwin
            arch: x86_64
            native: false
          - os: macos-14
            target: aarch64-apple-darwin
            platform: darwin
            arch: aarch64
            native: true

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - uses: Swatinem/rust-cache@v2

      - name: Install Linux cross toolchain
        if: matrix.target == 'aarch64-unknown-linux-gnu'
        run: |
          sudo apt-get update
          sudo apt-get install -y gcc-aarch64-linux-gnu binutils-aarch64-linux-gnu

      - name: Build release binary
        run: cargo build --release -p dust-cli --target "${{ matrix.target }}"
        env:
          AR_aarch64_unknown_linux_gnu: aarch64-linux-gnu-ar
          CC_aarch64_unknown_linux_gnu: aarch64-linux-gnu-gcc
          CARGO_TARGET_AARCH64_UNKNOWN_LINUX_GNU_LINKER: aarch64-linux-gnu-gcc

      - name: Package archive
        id: package
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p dist
          archive="dust-${GITHUB_REF_NAME}-${{ matrix.platform }}-${{ matrix.arch }}.tar.gz"

          binary=""
          for candidate in \
            "target/${{ matrix.target }}/release/dust" \
            "target/${{ matrix.target }}/release/dust-cli"; do
            if [ -f "$candidate" ]; then
              binary="$candidate"
              break
            fi
          done

          if [ -z "$binary" ]; then
            echo "release binary not found for ${{ matrix.target }}" >&2
            exit 1
          fi

          cp "$binary" dist/dust
          chmod +x dist/dust
          tar -C dist -czf "$archive" dust
          echo "archive=$archive" >> "$GITHUB_OUTPUT"

      - name: Run release parity smoke
        if: matrix.native
        run: cargo run -p xtask -- release-parity --archive "${{ steps.package.outputs.archive }}"

      - name: Upload release artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ steps.package.outputs.archive }}
          path: ${{ steps.package.outputs.archive }}

  release:
    needs: build
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          merge-multiple: true
          path: dist

      - name: Generate changelog
        shell: bash
        run: |
          set -euo pipefail

          previous_tag="$(git tag --merged "$GITHUB_SHA" --sort=-v:refname | grep -Fxv "$GITHUB_REF_NAME" | head -n 1 || true)"

          {
            echo "## Changelog"
            echo
            if [ -n "$previous_tag" ]; then
              echo "Changes since \`$previous_tag\`:"
              echo
              git log --no-merges --pretty='- %s (%h)' "${previous_tag}..${GITHUB_SHA}"
            else
              echo "- Initial release"
            fi
          } > CHANGELOG.md

      - name: Create GitHub release
        uses: softprops/action-gh-release@v2
        with:
          body_path: CHANGELOG.md
          files: dist/*.tar.gz
          name: ${{ github.ref_name }}
          prerelease: ${{ contains(github.ref_name, '-') }}
          tag_name: ${{ github.ref_name }}
